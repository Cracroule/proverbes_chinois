__author__ = 'rpil'

from setuptools import setup, find_packages
#from distutils.core import setup

setup(
    version='0.0.0.1',
    name='proverbes_chinois',
    description='Personal project about creating random chinese proverbs (in French)',
    # license='rpil',
    # package_dir={},
    # packages=['perudo'],
    packages=find_packages(),
    #scripts=['scripts/tournament.py'],
    install_requires=[],
)
